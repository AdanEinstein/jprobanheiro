{
  "name": "jprobanheiro",
  "version": "1.0.0",
  "private": "true",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "ws": "yarn workspace @jprobanheiro/socket start",
    "web": "cd ./apps/app && npx serve dist --single",
    "build:web": "cd ./apps/app && npx expo export -p web"
  }
}
